<%= @quiz.name %><hr>

<%= form_tag quizzes_path do %>
    <% @quiz.questions.each do |question| %>
        <%= question.name %><br>


        <%= collection_select :option, :option_id, question.options, :id, :name %><br>
        
    <% end %>
<%= submit_tag "Submit" %>
<% end %>